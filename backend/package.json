{
  "name": "kahoot-clone-backend",
  "version": "1.0.0",
  "description": "Backend para Kahoot Clone usando Node.js, Express y Socket.IO",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:frontend": "cd ../frontend && npm install && npm run build",
    "prepare:frontend": "node scripts/prepareFrontendBuild.js",
    "build:prod": "npm run build:frontend && npm run prepare:frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "mssql": "^11.0.1",
    "socket.io": "^4.7.5",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
